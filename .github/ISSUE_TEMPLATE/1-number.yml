name: A number looks wrong
description: A factor or adjustment seems off, is out of date, or needs a better source.
title: "Number looks wrong: "
labels: ["data", "factors"]
body:
  - type: markdown
    attributes:
      value: |
        Thanks for checking our work. Every number on the site should come from a public source, so the most useful thing you can give us is a link that backs up the right one.

        **This issue is public.** Please don't include your own premium, your VIN, a policy number, your name, or your address.
  - type: input
    id: factor
    attributes:
      label: Which number?
      description: A short name is plenty.
      placeholder: "e.g. the adjustment for a teen driver, or the $1,000 deductible"
    validations:
      required: true
  - type: input
    id: page
    attributes:
      label: Where did you see it?
      description: The page on the site, if you remember.
      placeholder: "e.g. / (the calculator) or /methodology"
  - type: input
    id: shown
    attributes:
      label: What the site says
      placeholder: "e.g. 1.25"
  - type: input
    id: suggested
    attributes:
      label: What you think it should be
      placeholder: "e.g. closer to 1.15"
  - type: input
    id: source_url
    attributes:
      label: Source link
      description: A public page that backs this up. State insurance departments, statutes, and government datasets are best.
      placeholder: "https://"
    validations:
      required: true
  - type: input
    id: source_checked
    attributes:
      label: When did you check the source?
      placeholder: "e.g. 2026-09-21"
  - type: textarea
    id: details
    attributes:
      label: Anything else?
      description: Where on the source page to look, how you'd work out the number, or anything we should know.
